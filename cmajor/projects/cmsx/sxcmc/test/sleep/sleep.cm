using System;

int main()
{
    try
    {
        Sleep(Duration.FromSeconds(3));
    }
    catch (const Exception& ex)
    {
        Console.Error() << ex.ToString() << endl();
        return 1;
    }
    return 0;
}
using System;

enum Foo
{
    alpha, beta, gamma
}

void foo()
{
    Foo foo = Foo.alpha;
    int x = 0;
    switch (foo)
    {
        case Foo.alpha: x = 0; break;
        case Foo.beta: x = 1; break;
        case Foo.gamma: x = 2; break;
    }
}
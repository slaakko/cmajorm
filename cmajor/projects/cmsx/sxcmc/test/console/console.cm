using System;

void main()
{
    try
    {
        Console.WriteLine();
        Console.WriteLine(1.23);
        Console.WriteLine(1.23f);
        Console.WriteLine(Now().Rep());
        Console.WriteLine(GetCurrentDate());
        Console.WriteLine(GetCurrentDateTime());
    }
    catch (const Exception& ex)
    {
        string s = ex.ToString();
        write(2, s.Chars(), s.Length());
    }
}

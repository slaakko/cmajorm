using System;

void main()
{
    try
    {
        Console.WriteLine();
        Console.WriteLine(1.23);
        Console.WriteLine(1.23f);
    }
    catch (const Exception& ex)
    {
        string s = ex.ToString();
        write(2, s.Chars(), s.Length());
    }
}

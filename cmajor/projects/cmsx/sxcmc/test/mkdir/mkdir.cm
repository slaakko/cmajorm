using System;
using System.Os;

int main()
{
    try
    {
        Mkdir("/bin");
    }
    catch (const Exception& ex)
    {
        Console.Error() << ex.ToString() << endl();
        return 1;
    }
    return 0;
}

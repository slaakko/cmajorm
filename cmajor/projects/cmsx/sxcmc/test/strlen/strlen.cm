using System;

public constexpr nothrow inline long SLen(const char* s)
{
    long len = 0;
    if (s != null)
    {
        while (*s != '\0')
        {
            ++len;
            ++s;
        }
    }
    return len;
}

void main()
{
    long n = SLen("abc");
}

using System;
using System.IO;

int main()
{
    try
    {
        System.Os.ChDir("/home/slaakko");
        string wd = GetCurrentWorkingDirectory();
        Console.WriteLine(wd);
    }
    catch (const Exception& ex)
    {
        Console.Error() << ex.ToString() << endl();
        return 1;
    }
    return 0;
}

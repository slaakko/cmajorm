using System;
using System.Collections;

void foo(const List<int>& list)
{
    for (int x : list)
    {
        Console.Out() << x << endl();
    }
}

void main()
{
    List<int> list;
    list.Add(1);
    list.Add(2);
    foo(list);
}

using System;
using Fibonacci;

int main()
{
    try
    {
        int f = fib(8); // 21
        print(f);
    }
    catch (const Exception& ex)
    {
        Console.Error() << ex.ToString() << endl();
        return 1;
    }
    return 0;
}

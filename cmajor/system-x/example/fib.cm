using System;

namespace Fibonacci
{
    public void print(int x)
    {
        Console.Out() << x << endl();
    }

    public int fib(int x)
    {
        if (x == 0) return 0;
        if (x == 1) return 1;
        return fib(x - 2) + fib(x - 1);
    }
}

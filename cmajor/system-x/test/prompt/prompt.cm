using System;
using System.Os;

void main()
{
	Console.Write(SetColors(ConsoleColor.green, DefaultConsoleBackColor()));
	Console.Write(GetUserName() + "@" + GetComputerName());
	Console.Write(ResetColors());
	Console.WriteLine();
}

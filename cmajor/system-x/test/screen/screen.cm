using System;

void main()
{
	int w = TermimalWindowWidth();
	int h = TermimalWindowHeight();
	int x = CursorX();
	int y = CursorY();
	SetCursorPos(0, h - 1);
	SetRaw(0);
	uchar c = ReadKey(0);
	if (c == keyDown)
	{
		Terminal.Out() << "Hip hip hurraa!";
		SetCursorPos(x, y);
	}
	else
	{
		Terminal.Out() << "nope!";
	}
}

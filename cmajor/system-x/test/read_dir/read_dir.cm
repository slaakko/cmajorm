using System;
using System.IO;

int main()
{
	try
	{
		DirectoryReader reader("/mnt/sx");
		DirectoryEntry entry;
		while (reader.Read(entry))
		{
			Console.WriteLine(entry.name);
		}
	}
	catch (const Exception& ex)
	{
		Console.Error() << ex.ToString() << endl();
		return 1;
	}
	return 0;
}
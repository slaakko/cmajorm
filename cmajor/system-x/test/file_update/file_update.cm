using System;
using System.IO;

int main()
{
	try
	{
		File.Copy("/mnt/cmajor/log/cmccs.log", "cmccs.log", cast<FileCopyOptions>(FileCopyOptions.update | FileCopyOptions.verbose));
	}
	catch (const Exception& ex)
	{
		Console.Error() << ex.ToString() << endl();
		return 1;
	}
	return 0;
}
